{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "switch-to-local-mfe",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Existing MFE Remote Submodule name"

    },
    "port": {
      "type": "number",
      "description": "Port on localhost"

    }
  },
  "required": ["name","port"]
}
